# Rendered by infra/setup.sh - do not edit this file.
# Change infra/infra.env and re-run setup.sh instead: every value the app and
# the audio chain share (mounts, ports, passwords) comes from that ONE file, so
# they cannot drift apart.

DATABASE_URL=file:${RADIO_ROOT}/db/dashboard.db

NEXTAUTH_URL=https://${DASHBOARD_HOSTNAME}
NEXTAUTH_SECRET=${NEXTAUTH_SECRET}

ICECAST_HOST=127.0.0.1
ICECAST_PORT=${ICECAST_PORT}
ICECAST_ADMIN_PASSWORD=${ICECAST_ADMIN_PASSWORD}
ICECAST_SOURCE_PASSWORD=${ICECAST_SOURCE_PASSWORD}
ICECAST_MOUNT=${ICECAST_MOUNT}
ICECAST_LIVE_MOUNT=${ICECAST_LIVE_MOUNT}
STREAM_HOSTNAME=${STREAM_HOSTNAME}

LIQUIDSOAP_HOST=127.0.0.1
LIQUIDSOAP_PORT=${LIQUIDSOAP_PORT}
LIQUIDSOAP_QUEUE=scheduled

UPLOADS_DIR=${RADIO_ROOT}/uploads
ARCHIVE_DIR=${RADIO_ROOT}/archive
FALLBACK_DIR=${RADIO_ROOT}/fallback

NODE_ENV=production
